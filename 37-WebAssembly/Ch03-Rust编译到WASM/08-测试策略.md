# 测试策略

## 一、概念说明

Rust WASM 项目需要在浏览器环境中测试。

```rust
// wasm-bindgen-test
use wasm_bindgen_test::*;

wasm_bindgen_test_configure!(run_in_browser);

#[wasm_bindgen_test]
fn test_add() {
    assert_eq!(add(1, 2), 3);
}
```

## 二、具体用法

### 2.1 基本测试

```rust
use wasm_bindgen_test::*;
use wasm_bindgen::prelude::*;

#[wasm_bindgen_test]
fn test_greet() {
    let result = greet("世界");
    assert_eq!(result, "你好，世界！");
}

#[wasm_bindgen_test]
async fn test_async_fetch() {
    let result = fetch_url("https://example.com").await;
    assert!(result.is_ok());
}
```

### 2.2 DOM 测试

```rust
use wasm_bindgen_test::*;
use web_sys::HtmlElement;

wasm_bindgen_test_configure!(run_in_browser);

#[wasm_bindgen_test]
fn test_create_element() {
    let document = web_sys::window().unwrap().document().unwrap();
    let element = document.create_element("div").unwrap();
    element.set_text_content(Some("测试内容"));
    assert_eq!(element.text_content().unwrap(), "测试内容");
}
```

### 2.3 集成测试

```rust
// tests/integration.rs
use wasm_bindgen_test::*;
use my_wasm_lib::*;

wasm_bindgen_test_configure!(run_in_browser);

#[wasm_bindgen_test]
async fn test_full_workflow() {
    let result = process_workflow("输入").await;
    assert!(result.is_ok());
}
```

### 2.4 运行测试

```bash
# 运行所有测试
wasm-pack test --headless --chrome

# 运行特定测试
wasm-pack test --headless --chrome -- --test test_add

# 在浏览器中运行
wasm-pack test --chrome
```

## 三、注意事项与常见陷阱

1. **浏览器环境**：测试需要浏览器环境
2. **异步测试**：使用 async 标记异步测试
3. **DOM 测试**：DOM 测试需要 run_in_browser
4. **CI 集成**：CI 中使用 headless 浏览器
5. **测试覆盖**：确保测试覆盖关键路径
