# 部署与发布

## 一、概念说明

Rust WASM 项目的部署和发布流程。

```bash
# 发布到 npm
wasm-pack build --target bundler
cd pkg
npm publish
```

## 二、具体用法

### 2.1 发布到 npm

```json
// package.json
{
  "name": "my-wasm-lib",
  "version": "1.0.0",
  "description": "Rust WASM 库",
  "main": "my_wasm_lib.js",
  "types": "my_wasm_lib.d.ts",
  "files": [
    "my_wasm_lib_bg.wasm",
    "my_wasm_lib.js",
    "my_wasm_lib.d.ts"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/user/repo"
  },
  "license": "MIT"
}
```

### 2.2 CDN 部署

```bash
# 构建
wasm-pack build --target web --release

# 上传到 CDN
# Cloudflare, AWS S3, etc.

# 在 HTML 中引用
<script type="module">
  import init from 'https://cdn.example.com/my-wasm-lib/my_wasm_lib.js';
  await init();
</script>
```

### 2.3 版本管理

```bash
# 更新版本
cargo set-version 1.2.3
git add -A
git commit -m "release: v1.2.3"
git tag v1.2.3
git push && git push --tags

# 发布
wasm-pack build --release --target web
cd pkg
npm version patch
npm publish
```

### 2.4 CI/CD

```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: wasm32-unknown-unknown
      - name: Install wasm-pack
        run: curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh
      - name: Build
        run: wasm-pack build --release --target web
      - name: Publish
        run: cd pkg && npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

## 三、注意事项与常见陷阱

1. **版本同步**：Cargo.toml 和 package.json 版本同步
2. **许可证**：确保许可证正确
3. **文档**：维护好文档
4. **安全**：注意安全更新
5. **监控**：监控发布后的使用情况
