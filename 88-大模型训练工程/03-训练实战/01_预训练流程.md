# 预训练流程 - 大模型训练工程

*数据配比 / Tokenizer 训练 / 超参数选择 / Checkpoint 管理*


## 预训练流程


数据配比 / Tokenizer 训练 / 超参数选择 / Checkpoint 管理

[88-大模型训练工程](../index.html)
>
[03-训练实战](./)
>
            01_预训练流程

### 目录


1. [预训练整体流程](#pipeline)
2. [数据准备与配比](#data)
3. [Tokenizer 训练](#tokenizer)
4. [预训练超参数](#hyperparams)
5. [Checkpoint 管理](#checkpoint)
6. [训练恢复 (Resume)](#resume)
7. [Scaling Laws 应用](#scaling-laws)


## 1. 预训练整体流程


```
大模型预训练 Pipeline:

┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
│  数据收集  │──►│ 数据清洗  │──►│Tokenizer │──►│ 数据预处理│
│  爬虫/API │   │ 去重/过滤 │   │  训练    │   │ Tokenize │
└──────────┘   └──────────┘   └──────────┘   └───┬──────┘
                                                   │
┌──────────┐   ┌──────────┐   ┌──────────┐        │
│ 模型评估  │◄──│ 预训练    │◄──│ 模型架构  │◄───────┘
│ 下游评测  │   │ 训练循环  │   │  设计    │
└──────────┘   └──────────┘   └──────────┘

关键阶段:
1. 数据: 数量(Tokens) + 质量(过滤) + 多样性(配比)
2. Tokenizer: 词表大小 + 特殊token + 多语言支持
3. 架构: 层数 + 维度 + 注意力头 + 位置编码
4. 超参: 学习率 + batch size + 训练步数
5. 工程: 并行策略 + 混合精度 + checkpoint
```


## 2. 数据准备与配比


### 2.1 数据来源


| 数据源 | 规模 | 质量 | 说明 |
| --- | --- | --- | --- |
| Common Crawl | PB 级 | 低（需大量清洗） | 网页爬虫，最大的公开数据集 |
| 书籍 | TB 级 | 高 | Books3, Gutenberg, Project Gutenberg |
| 学术论文 | TB 级 | 非常高 | arXiv, S2ORC, PubMed |
| 代码 | TB 级 | 高（结构化） | GitHub, StackOverflow, The Stack |
| 百科/问答 | GB 级 | 高 | Wikipedia, StackExchange |


### 2.2 数据清洗流程


```
python
# 数据清洗 Pipeline
import hashlib
from datasketch import MinHash, MinHashLSH

class DataCleaningPipeline:
    def __init__(self):
        self.lsh = MinHashLSH(threshold=0.8, num_perm=128)

    def clean_text(self, text):
        # 1. 基础清洗
        text = remove_html_tags(text)
        text = remove_urls(text)
        text = normalize_whitespace(text)

        # 2. 长度过滤
if len(text) < 50 or len(text) > 100000:
            return None
# 3. 语言检测
        lang = detect_language(text)
        if lang not in self.target_langs:
            return None
# 4. 质量分类器 (perplexity 过滤)
        perplexity = compute_perplexity(text)
        if perplexity > self.max_perplexity:
            return None
return text

    def dedup(self, texts):
        # MinHash + LSH 近似去重
        unique_texts = []
        for text in texts:
            m = MinHash(num_perm=128)
            for word in text.split():
                m.update(word.encode('utf-8'))
            result = self.lsh.query(m)
            if not result:
                self.lsh.insert(hash(text), m)
                unique_texts.append(text)
        return unique_texts
```


### 2.3 数据配比（各家实践）


| 模型 | 网页 | 书籍 | 代码 | 学术 | 百科 | 其他 |
| --- | --- | --- | --- | --- | --- | --- |
| LLaMA (7B) | 67% | 4.5% | 4.5% | 2.5% | 4.5% | 17% |
| LLaMA 2 | 81.5% | 2.6% | 4.1% | 4.5% | 2.4% | 4.9% |
| GPT-3 | 60% | 16% | 0% | 0% | 3% | 21% |
| PaLM | 27% | 14% | 5% | 3% | 4% | 47% |


> **Note:** **数据配比经验：**
>
> - 网页数据占比最大（50-80%），但需要严格过滤
> - 代码数据对推理能力提升显著（LLaMA 建议 5-10%）
> - 高质量数据（书籍、论文）虽占比小，但对模型能力影响大
> - 重复数据需要多次 epoch 时，建议在数据混合阶段而非 epoch 维度重复


## 3. Tokenizer 训练


### 3.1 主流分词算法


| 算法 | 原理 | 代表模型 | 优势 |
| --- | --- | --- | --- |
| BPE | 贪心合并最高频对 | GPT, LLaMA, Qwen | 简单高效，效果好 |
| Unigram (SPM) | 基于概率的最优子词 | T5, mBART | 多语言友好 |
| WordPiece | 似然最大化选择 | BERT | 适合单语言 |


### 3.2 BPE 训练


```
python
# 使用 HuggingFace Tokenizers 训练 BPE
from tokenizers import Tokenizer, models, trainers, pre_tokenizers

# 创建 BPE Tokenizer
tokenizer = Tokenizer(models.BPE(unk_token="[UNK]"))

# 设置预分词 (按空格和标点分割)
tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)

# 定义特殊 Token
special_tokens = ["[PAD]", "[UNK]", "[CLS]", "[SEP]", "[MASK]",
                  "<|endoftext|>", "<|im_start|>", "<|im_end|>"]

# 训练
trainer = trainers.BpeTrainer(
    vocab_size=32000,           # 词表大小 (常用: 32K/64K/128K)
    min_frequency=2,            # 最小频次
    special_tokens=special_tokens,
    show_progress=True,
    initial_alphabet=pre_tokenizers.ByteLevel.alphabet()
)

tokenizer.train(files=["corpus.txt"], trainer=trainer)
tokenizer.save("tokenizer.json")
```


### 3.3 词表大小选择


| 词表大小 | 效果 | 适用场景 |
| --- | --- | --- |
| 32K | 每 Token 信息量少 | 英文为主, T5 |
| 64K | 平衡效率与效果 | 多语言, LLaMA |
| 128K | 每 Token 信息量大 | 中文友好, Qwen |
| 256K+ | 特殊需求 | 极多语言 |


## 4. 预训练超参数


### 4.1 关键超参数参考


| 超参数 | LLaMA-7B | LLaMA-13B | LLaMA-70B | 选择原则 |
| --- | --- | --- | --- | --- |
| 学习率 (lr_max) | 3e-4 | 3e-4 | 1.5e-4 | 规模越大, lr 越小 |
| Batch Size (tokens) | 4M | 4M | 4M | 通常 2-4M tokens |
| 训练 Tokens | 1T | 1T | 1.4T | Chinchilla: 20× 参数量 |
| Adam β₁ | 0.9 | 0.9 | 0.9 | 通常固定 0.9 |
| Adam β₂ | 0.95 | 0.95 | 0.95 | 通常 0.95 ~ 0.99 |
| Weight Decay | 0.1 | 0.1 | 0.1 | 通常 0.1 |
| Warmup Steps | 2000 | 2000 | 2000 | 1-2% 总步数 |
| 梯度裁剪 | 1.0 | 1.0 | 1.0 | 通常 1.0 |
| 上下文长度 | 2048 | 2048 | 4096 | 2K ~ 128K |


### 4.2 训练步数计算


$$
总步数 = 总训练 Tokens / (batch_size × seq_length × dp_size)
                训练 FLOPs ≈ 6 × N × D (N=参数量, D=训练tokens)
$$


```
python
# 训练步数和时间估算
num_params = 7e9           # 7B 参数
total_tokens = 1e12         # 1T tokens
global_batch_tokens = 4e6   # 4M tokens per step
num_gpus = 64
gpu_tflops = 312 # A100 BF16 TFLOPS
mfu_target = 0.45 # 目标 MFU (45%)
# 步数
total_steps = total_tokens / global_batch_tokens
print(f"总步数: {total_steps:.0f}")  # 250,000 步
# FLOPs
total_flops = 6 * num_params * total_tokens
print(f"总 FLOPs: {total_flops:.2e}")  # 4.2e19
# 时间估算
step_flops = 6 * num_params * global_batch_tokens
step_time = step_flops / (gpu_tflops * 1e12 * num_gpus * mfu_target)
total_hours = step_time * total_steps / 3600
print(f"每步时间: {step_time:.2f}s")
print(f"总训练时间: {total_hours:.0f}h ({total_hours/24:.1f}天)")
```


## 5. Checkpoint 管理


### 5.1 Checkpoint 策略


- **定期保存：**
   每 N 步保存一次（如每 1000 步）
- **保留策略：**
   保留最近 K 个 + 最佳 N 个 checkpoint
- **异步保存：**
   后台线程保存，不阻塞训练
- **增量保存：**
   分布式 checkpoint（DCP）分片保存


### 5.2 Checkpoint 内容


```
标准 Checkpoint 结构:

checkpoint-10000/
├── model.safetensors          # 模型权重
├── optimizer.pt               # 优化器状态 (可选)
├── scheduler.pt               # 学习率调度器状态
├── trainer_state.json         # 训练器状态 (当前步数等)
├── rng_state.pth              # 随机数状态
├── config.json                # 模型配置
└── tokenizer.json             # Tokenizer
```


```
python
# Checkpoint 保存与加载
import torch
from torch.distributed.checkpoint import save, load, FileSystemWriter, FileSystemReader

# 方法一: 标准保存
torch.save({
    'model_state_dict': model.state_dict(),
    'optimizer_state_dict': optimizer.state_dict(),
    'scheduler_state_dict': scheduler.state_dict(),
    'epoch': epoch,
    'step': global_step,
    'best_loss': best_loss,
}, f'checkpoint-{global_step}.pt')

# 方法二: 分布式 Checkpoint (推荐)
from torch.distributed.checkpoint import save, load, StorageWriter

state_dict = {
    "model": model.state_dict(),
    "optimizer": optimizer.state_dict(),
}
save(state_dict, storage_writer=FileSystemWriter(f"ckpt-{step}"))

# 加载
load(state_dict, storage_reader=FileSystemReader("ckpt-10000"))
model.load_state_dict(state_dict["model"])
```


## 6. 训练恢复 (Resume)


### 6.1 恢复流程


```
python
def resume_training(checkpoint_path, model, optimizer, scheduler):
    # 1. 加载 checkpoint
    ckpt = torch.load(checkpoint_path, map_location="cpu")

    # 2. 恢复模型权重
    model.load_state_dict(ckpt['model_state_dict'])

    # 3. 恢复优化器状态
    optimizer.load_state_dict(ckpt['optimizer_state_dict'])

    # 4. 恢复学习率调度器
    scheduler.load_state_dict(ckpt['scheduler_state_dict'])

    # 5. 恢复训练状态
    start_step = ckpt['step']
    best_loss = ckpt.get('best_loss', float('inf'))

    # 6. 恢复随机数状态 (确保可复现)
    torch.set_rng_state(ckpt['rng_state'])
    torch.cuda.set_rng_state_all(ckpt['cuda_rng_state'])

    return start_step, best_loss
```


> **Warning:** **常见问题：**
>
> - 恢复后 loss 突变：检查是否加载了正确的 optimizer state
> - 分布式训练恢复：确保所有 rank 加载相同 checkpoint
> - 学习率异常：scheduler 状态未正确恢复


## 7. Scaling Laws 应用


### 7.1 Chinchilla 定律


$$
最优 Token 数 ≈ 20 × 模型参数量
                最优计算量: C = 6 × N × D
$$


| 模型参数量 | 最优 Token 数 (Chinchilla) | 实际 Token 数 | 说明 |
| --- | --- | --- | --- |
| 7B | 140B | 1T (LLaMA) | 过度训练，换取小模型高性能 |
| 13B | 260B | 1T | 同上 |
| 70B | 1.4T | 1.4T (LLaMA 2) | 接近 Chinchilla 最优 |


> **Note:** **实际应用：**
> Chinchilla 原则在推理成本敏感的场景下需要调整。训练一个 7B 模型到 1T tokens（而非训练 70B 到 140B tokens）虽然不是 compute-optimal，但推理成本低得多，性价比可能更高。这就是 LLaMA 选择 "over-train" 小模型的原因。

大模型训练工程 - 预训练流程 | 最后更新: 2025年


<!-- Converted from: 01_预训练流程.html -->
