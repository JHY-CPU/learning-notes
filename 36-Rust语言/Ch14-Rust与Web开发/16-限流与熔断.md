# 限流与熔断

## 一、概念说明

限流控制请求速率，熔断在服务不稳定时快速失败，防止级联故障。

```rust
use governor::{Quota, RateLimiter};
use std::num::NonZeroU32;

let limiter = RateLimiter::direct(Quota::per_second(NonZeroU32::new(100).unwrap()));
```

## 二、具体用法

### 2.1 限流中间件

```rust
use tower::limit::RateLimitLayer;
use std::time::Duration;

let app = Router::new()
    .route("/", get(index))
    .layer(RateLimitLayer::new(100, Duration::from_secs(1)));
```

### 2.2 熔断器

```rust
use tokio::sync::Semaphore;
use std::sync::atomic::{AtomicU32, Ordering};

struct CircuitBreaker {
    failures: AtomicU32,
    threshold: u32,
}

impl CircuitBreaker {
    fn call<F, T>(&self, f: F) -> Option<T>
    where
        F: FnOnce() -> T,
    {
        if self.failures.load(Ordering::Relaxed) >= self.threshold {
            return None; // 熔断
        }

        match std::panic::catch_unwind(std::panic::AssertUnwindSafe(f)) {
            Ok(result) => {
                self.failures.store(0, Ordering::Relaxed);
                Some(result)
            }
            Err(_) => {
                self.failures.fetch_add(1, Ordering::Relaxed);
                None
            }
        }
    }
}
```

## 三、注意事项与常见陷阱

1. **粒度选择**：全局 vs 用户 vs IP 限流
2. **分布式限流**：使用 Redis 实现分布式限流
3. **熔断恢复**：实现熔断器的半开状态
4. **监控告警**：监控限流和熔断触发
5. **用户体验**：限流时返回友好的错误信息
