# 游戏开发入门

## 一、概念说明

Rust 在游戏开发中越来越受欢迎，Bevy 是最流行的 Rust 游戏引擎。

```rust
use bevy::prelude::*;

fn main() {
    App::new()
        .add_plugins(DefaultPlugins)
        .add_systems(Startup, setup)
        .add_systems(Update, move_player)
        .run();
}

fn setup(mut commands: Commands) {
    commands.spawn(Camera2dBundle::default());
}
```

## 二、具体用法

### 2.1 ECS 架构

```rust
use bevy::prelude::*;

#[derive(Component)]
struct Player;

#[derive(Component)]
struct Velocity(Vec2);

fn movement_system(
    mut query: Query<(&mut Transform, &Velocity), With<Player>>,
    time: Res<Time>,
) {
    for (mut transform, velocity) in &mut query {
        transform.translation.x += velocity.0.x * time.delta_seconds();
        transform.translation.y += velocity.0.y * time.delta_seconds();
    }
}
```

### 2.2 输入处理

```rust
fn keyboard_input(
    keyboard: Res<Input<KeyCode>>,
    mut query: Query<&mut Velocity, With<Player>>,
) {
    let mut velocity = query.single_mut();
    if keyboard.pressed(KeyCode::W) {
        velocity.0.y = 100.0;
    }
    if keyboard.pressed(KeyCode::S) {
        velocity.0.y = -100.0;
    }
}
```

### 2.3 碰撞检测

```rust
use bevy::prelude::*;

fn collision_detection(
    query1: Query<&Transform, With<Player>>,
    query2: Query<&Transform, With<Enemy>>,
) {
    let player = query1.single();
    for enemy in &query2 {
        let distance = player.translation.distance(enemy.translation);
        if distance < 50.0 {
            println!("碰撞！");
        }
    }
}
```

## 三、注意事项与常见陷阱

1. **ECS 学习曲线**：ECS 架构需要时间理解
2. **性能优化**：合理使用查询和系统
3. **资产管线**：管理游戏资产
4. **物理引擎**：使用 bevy_rapier 处理物理
5. **社区资源**：利用 Bevy 社区资源学习
