# 内存淘汰策略

## 一、概念说明

当Redis内存达到maxmemory限制时，根据淘汰策略删除部分key以腾出空间。选择合适的淘汰策略对应用性能至关重要。

## 二、淘汰策略

```bash
# 八种淘汰策略

# 1. noeviction（默认）
#    内存满时返回错误，不删除任何key
maxmemory-policy noeviction

# 2. allkeys-lru
#    从所有key中淘汰最近最少使用的
maxmemory-policy allkeys-lru

# 3. volatile-lru
#    从有过期时间的key中淘汰LRU
maxmemory-policy volatile-lru

# 4. allkeys-random
#    从所有key中随机淘汰
maxmemory-policy allkeys-random

# 5. volatile-random
#    从有过期时间的key中随机淘汰
maxmemory-policy volatile-random

# 6. volatile-ttl
#    从有过期时间的key中淘汰TTL最小的
maxmemory-policy volatile-ttl

# 7. allkeys-lfu（Redis 4.0+）
#    从所有key中淘汰最不常用的
maxmemory-policy allkeys-lfu

# 8. volatile-lfu（Redis 4.0+）
#    从有过期时间的key中淘汰LFU
maxmemory-policy volatile-lfu
```

### 策略选择

```bash
# 缓存场景推荐
# allkeys-lru：所有key都可能被淘汰
# allkeys-lfu：基于访问频率淘汰

# 混合场景推荐
# volatile-lru：只淘汰设了过期时间的key
# volatile-lfu：基于频率淘汰过期key

# 严格场景
# noeviction：不自动淘汰，内存满报错
```

## 三、配置与监控

```bash
# 设置最大内存
maxmemory 1gb

# 设置淘汰策略
maxmemory-policy allkeys-lru

# 查看内存使用
INFO memory
# used_memory_human:1.00G
# maxmemory_human:1.00G

# 查看淘汰数量
INFO stats
# evicted_keys:1000
```

## 四、注意事项

1. **必须设置maxmemory**：否则可能耗尽系统内存
2. **LRU是近似算法**：采样方式，不是精确LRU
3. **allkeys vs volatile**：volatile只淘汰有过期时间的key
4. **LFU需要4.0+**：旧版本不支持LFU策略
5. **淘汰有性能开销**：大量淘汰会影响性能
