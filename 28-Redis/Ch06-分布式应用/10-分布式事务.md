# 分布式事务

## 一、概念说明

分布式事务保证跨多个服务/数据源的操作要么全部成功，要么全部失败。Redis可用于实现分布式事务中的部分方案。

## 二、最终一致性方案

### 可靠消息最终一致性

```bash
# 1. 本地事务写入业务数据
# 2. 写入消息表
# 3. 发送到Redis消息队列
LPUSH tx:messages '{"id":"tx001","action":"update_inventory","data":{...}}'

# 4. 消费者处理消息
BRPOP tx:messages 0

# 5. 处理成功后删除消息
# 6. 处理失败重试
```

### TCC模式

```python
# Try阶段
def try_reserve(order_id, amount):
    # 冻结资金
    r.hincrby(f"account:{order_id}", "frozen", amount)
    r.hincrby(f"account:{order_id}", "balance", -amount)
    return True

# Confirm阶段
def confirm(order_id, amount):
    r.hincrby(f"account:{order_id}", "frozen", -amount)
    return True

# Cancel阶段
def cancel(order_id, amount):
    r.hincrby(f"account:{order_id}", "frozen", -amount)
    r.hincrby(f"account:{order_id}", "balance", amount)
    return True
```

## 三、Saga模式

```bash
# 顺序执行，失败时补偿
# 步骤1: 扣减库存
DECR inventory:1001

# 步骤2: 创建订单
LPUSH orders '{"id":1001}'

# 步骤3失败 → 补偿步骤1
INCR inventory:1001
```

## 四、Redis事务 + Lua

```bash
# 使用Lua保证原子性
EVAL "
  local balance = tonumber(redis.call('hget', KEYS[1], 'balance'))
  local amount = tonumber(ARGV[1])
  if balance >= amount then
    redis.call('hincrby', KEYS[1], 'balance', -amount)
    redis.call('hincrby', KEYS[2], 'balance', amount)
    return 1
  else
    return 0
  end
" 2 account:1 account:2 100
```

## 五、注意事项

1. **Redis不支持ACID事务**：只保证命令原子性
2. **最终一致性**：大多数场景接受最终一致
3. **补偿机制**：失败时需要补偿操作
4. **消息可靠**：确保消息不丢失
5. **幂等性**：所有操作必须幂等
