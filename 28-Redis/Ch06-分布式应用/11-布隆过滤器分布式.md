# 布隆过滤器分布式

## 一、概念说明

Redis布隆过滤器用于分布式系统中的去重和存在性判断，内存效率极高，有极小的误判率。

## 二、基本操作

```bash
# 加载模块
MODULE LOAD redisbloom.so

# 创建布隆过滤器
BF.RESERVE myfilter 0.01 1000000
# 0.01: 1%误判率
# 1000000: 预计100万元素

# 添加元素
BF.ADD myfilter "user:1001"
BF.MADD myfilter "user:1002" "user:1003"

# 检查元素
BF.EXISTS myfilter "user:1001"  # 1(可能存在)
BF.EXISTS myfilter "user:9999"  # 0(一定不存在)
```

## 三、应用场景

```python
# 防止缓存穿透
def get_user(user_id):
    # 1. 检查布隆过滤器
    if not bf.exists(f"users", f"user:{user_id}"):
        return None
    
    # 2. 查缓存
    user = redis.get(f"user:{user_id}")
    if user:
        return user
    
    # 3. 查数据库
    user = db.query(user_id)
    if user:
        redis.set(f"user:{user_id}", user, ex=3600)
    else:
        # 缓存空值防止穿透
        redis.set(f"user:{user_id}", "NULL", ex=300)
    return user

# 新用户注册时添加到布隆过滤器
def register_user(user_id):
    bf.add("users", f"user:{user_id}")
    # 保存到数据库...
```

## 四、Cuckoo Filter

```bash
# 支持删除的布隆过滤器
CF.RESERVE mycuckoo 1000000
CF.ADD mycuckoo "item1"
CF.EXISTS mycuckoo "item1"  # 1
CF.DEL mycuckoo "item1"
CF.EXISTS mycuckoo "item1"  # 0
```

## 五、注意事项

1. **误判率**：存在一定误判，可能误判存在
2. **不支持删除**：标准布隆过滤器不支持
3. **容量规划**：预估准确的元素数量
4. **集群部署**：所有节点共享同一个布隆过滤器
5. **内存效率**：比Set节省90%以上内存
