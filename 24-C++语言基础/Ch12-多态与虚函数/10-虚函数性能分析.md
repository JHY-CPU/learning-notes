# 虚函数性能分析

## 一、概念说明

虚函数调用通过vtable间接跳转，比直接调用多一次指针解引用。但在现代CPU上，这个开销通常可以忽略。编译器还可以通过**devirtualization**（去虚化）优化消除虚调用开销。

## 二、具体用法

### 2.1 性能对比

```cpp
#include <iostream>
#include <chrono>

class Base {
public:
    virtual int virtualFunc(int x) { return x * 2; }
    int directFunc(int x) { return x * 2; }
};

class Derived : public Base {
public:
    int virtualFunc(int x) override { return x * 3; }
};

int main() {
    Derived d;
    Base* p = &d;
    const int N = 100000000;
    volatile int result = 0;

    // 虚函数调用
    auto start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < N; ++i)
        result = p->virtualFunc(i);
    auto end = std::chrono::high_resolution_clock::now();
    auto vtime = std::chrono::duration_cast<std::chrono::milliseconds>(end - start);

    // 直接调用
    start = std::chrono::high_resolution_clock::now();
    for (int i = 0; i < N; ++i)
        result = d.directFunc(i);
    end = std::chrono::high_resolution_clock::now();
    auto dtime = std::chrono::duration_cast<std::chrono::milliseconds>(end - start);

    std::cout << "虚函数: " << vtime.count() << "ms" << std::endl;
    std::cout << "直接调用: " << dtime.count() << "ms" << std::endl;
    return 0;
}
```

## 三、注意事项与常见陷阱

- 虚函数的开销主要是**间接跳转**（一次指针解引用）
- 现代CPU分支预测通常能很好处理虚调用
- `final`类/函数允许编译器devirtualize
- 内联在虚函数中通常不可能（编译器不知道实际类型）
- 性能关键路径考虑模板（静态多态）替代
