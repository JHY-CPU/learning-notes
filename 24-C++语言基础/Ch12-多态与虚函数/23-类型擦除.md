# 类型擦除

## 一、概念说明

**类型擦除**（Type Erasure）将不同类型对象的共同行为通过统一接口暴露，隐藏具体类型信息。`std::function`、`std::any`都是类型擦除的典型应用。

## 二、具体用法

### 2.1 手动实现类型擦除

```cpp
#include <iostream>
#include <memory>
#include <string>

class AnyDrawable {
    struct Concept {
        virtual ~Concept() = default;
        virtual void draw() const = 0;
        virtual std::unique_ptr<Concept> clone() const = 0;
    };

    template<typename T>
    struct Model : Concept {
        T value;
        Model(const T& v) : value(v) {}
        void draw() const override { value.draw(); }
        std::unique_ptr<Concept> clone() const override {
            return std::make_unique<Model>(value);
        }
    };

    std::unique_ptr<Concept> ptr;
public:
    template<typename T>
    AnyDrawable(const T& v) : ptr(std::make_unique<Model<T>>(v)) {}

    AnyDrawable(const AnyDrawable& o) : ptr(o.ptr->clone()) {}

    void draw() const { ptr->draw(); }
};

struct Circle { void draw() const { std::cout << "圆形" << std::endl; } };
struct Square { void draw() const { std::cout << "方形" << std::endl; } };

int main() {
    AnyDrawable d1(Circle{});
    AnyDrawable d2(Square{});
    d1.draw();
    d2.draw();
    return 0;
}
```

**输出：**
```
圆形
方形
```

## 三、注意事项与常见陷阱

- 类型擦除有运行时开销（虚函数调用+堆分配）
- `std::function`内部也是类型擦除实现
- 类型擦除提供运行时多态的灵活性
- 注意自定义类型擦除的拷贝语义
