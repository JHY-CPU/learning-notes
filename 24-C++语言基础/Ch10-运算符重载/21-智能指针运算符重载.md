# 智能指针运算符重载

## 一、概念说明

智能指针需要重载`*`（解引用）、`->`（成员访问）、`=`（赋值）等运算符，模拟裸指针的行为同时自动管理资源生命周期。

## 二、具体用法

### 2.1 unique_ptr简化实现

```cpp
#include <iostream>

template<typename T>
class UniquePtr {
    T* ptr;
public:
    explicit UniquePtr(T* p = nullptr) : ptr(p) {}
    ~UniquePtr() { delete ptr; }

    // 禁止拷贝
    UniquePtr(const UniquePtr&) = delete;
    UniquePtr& operator=(const UniquePtr&) = delete;

    // 移动
    UniquePtr(UniquePtr&& o) noexcept : ptr(o.ptr) { o.ptr = nullptr; }
    UniquePtr& operator=(UniquePtr&& o) noexcept {
        if (this != &o) { delete ptr; ptr = o.ptr; o.ptr = nullptr; }
        return *this;
    }

    // 解引用
    T& operator*() const { return *ptr; }
    T* operator->() const { return ptr; }

    // 布尔转换
    explicit operator bool() const { return ptr != nullptr; }

    // 相等比较
    bool operator==(const UniquePtr& o) const { return ptr == o.ptr; }
    bool operator!=(const UniquePtr& o) const { return ptr != o.ptr; }

    T* get() const { return ptr; }
    T* release() { T* p = ptr; ptr = nullptr; return p; }
};

struct Point { int x, y; };

int main() {
    UniquePtr<Point> p1(new Point{3, 4});
    std::cout << "p1->x=" << p1->x << ", (*p1).y=" << (*p1).y << std::endl;

    UniquePtr<Point> p2 = std::move(p1);
    std::cout << "p1为空: " << !p1 << std::endl;
    std::cout << "p2->x=" << p2->x << std::endl;
    return 0;
}
```

**输出：**
```
p1->x=3, (*p1).y=4
p1为空: 1
p2->x=3
```

## 三、注意事项与常见陷阱

- `operator->`的特殊递归语义：返回指针后编译器自动`->`
- `explicit operator bool()`防止意外与整数比较
- `unique_ptr`禁止拷贝，`shared_ptr`允许拷贝增加引用计数
- 实际项目中使用`std::unique_ptr`和`std::shared_ptr`
