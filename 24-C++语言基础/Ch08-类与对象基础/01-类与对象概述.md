# 类与对象概述

## 一、概念说明

**类**（Class）是C++面向对象编程的核心，是用户自定义类型的蓝图，封装了**数据成员**（属性）和**成员函数**（方法）。**对象**（Object）是类的实例。

OOP四大特性：**封装**（隐藏实现细节）、**继承**（复用已有代码）、**多态**（统一接口不同实现）、**抽象**（提取共同特征）。

## 二、基本概念

### 2.1 类的定义与实例化

```cpp
class Dog {
public:
    std::string name;
    int age;

    void bark() {
        std::cout << name << " says: Woof!" << std::endl;
    }
};

Dog myDog;           // 实例化对象
myDog.name = "Buddy";
myDog.age = 3;
myDog.bark();
// 输出: Buddy says: Woof!
```

### 2.2 封装

```cpp
class BankAccount {
private:
    double balance;  // 私有数据

public:
    void deposit(double amount) {
        if (amount > 0) balance += amount;
    }
    double getBalance() const { return balance; }
};

BankAccount acc;
acc.deposit(1000);
std::cout << "余额: " << acc.getBalance() << std::endl;
// 输出: 余额: 1000
```

### 2.3 class vs struct

```cpp
// class：默认private
class MyClass {
    int x;  // private
};

// struct：默认public
struct MyStruct {
    int x;  // public
};

MyStruct s;
s.x = 42;  // OK

// MyClass m;
// m.x = 42;  // 编译错误：x是private
```

## 三、本章内容概览

- **基础篇**：定义、访问控制、this指针、构造/析构函数
- **特殊成员**：拷贝构造/赋值、静态成员、友元
- **高级特性**：委托构造、explicit、default/delete
- **设计原则**：不变式、PIMPL、SOLID
- **模板与多态**：类模板、CRTP、值/引用语义
