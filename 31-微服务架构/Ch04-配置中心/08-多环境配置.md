# 多环境配置

## 一、环境隔离

```yaml
# 命名空间隔离
spring:
  profiles:
    active: dev  # dev / test / prod
  cloud:
    nacos:
      config:
        namespace: ${spring.profiles.active}
```

## 二、注意事项

1. **命名空间隔离不同环境**
2. **配置文件按环境分组**
3. **生产配置严格管控**
4. **环境切换要方便**
5. **配置差异要最小化**
