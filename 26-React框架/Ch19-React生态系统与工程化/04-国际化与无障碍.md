# 国际化与无障碍

## 一、国际化 (i18n)

### 1.1 方案对比

```
┌────────────────┬──────────┬──────────┬──────────┐
│                │react-intl│react-i18n│ next-intl│
├────────────────┼──────────┼──────────┼──────────┤
│ 生态           │ 成熟     │ 最流行   │ Next.js  │
│ 包大小         │ 较大     │ 中等     │ 中等     │
│ TypeScript     │ 一般     │ 好       │ 优秀     │
│ ICU Message    │ ✅       │ ✅       │ ✅       │
│ 懒加载         │ ✅       │ ✅       │ ✅       │
│ SSR            │ ✅       │ ✅       │ ✅       │
│ 推荐场景       │ 大型项目 │ 通用     │ Next.js  │
└────────────────┴──────────┴──────────┴──────────┘
```

---

## 二、react-i18next

### 2.1 安装配置

```bash
npm install react-i18next i18next
npm install i18next-browser-languagedetector  # 自动检测语言
npm install i18next-http-backend              # 懒加载翻译文件
```

```typescript
// src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import Backend from 'i18next-http-backend';

i18n
  .use(Backend)          // 从 /public/locales 加载翻译文件
  .use(LanguageDetector) // 检测用户语言
  .use(initReactI18next) // 传递给 react-i18next
  .init({
    fallbackLng: 'zh-CN',      // 默认语言
    debug: import.meta.env.DEV,

    interpolation: {
      escapeValue: false,  // React 已转义
    },

    // 支持的语言
    supportedLngs: ['zh-CN', 'en', 'ja'],

    // 命名空间
    ns: ['common', 'home', 'auth'],
    defaultNS: 'common',

    // 后端配置
    backend: {
      loadPath: '/locales/{{lng}}/{{ns}}.json',
    },

    // 语言检测配置
    detection: {
      order: ['querystring', 'cookie', 'localStorage', 'navigator'],
      caches: ['localStorage', 'cookie'],
      lookupQuerystring: 'lng',
      lookupCookie: 'i18next',
      lookupLocalStorage: 'i18nextLng',
    },
  });

export default i18n;
```

```typescript
// src/main.tsx
import './i18n';  // 在应用之前初始化

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

### 2.2 翻译文件结构

```
public/locales/
├── zh-CN/
│   ├── common.json
│   ├── home.json
│   └── auth.json
├── en/
│   ├── common.json
│   ├── home.json
│   └── auth.json
└── ja/
    ├── common.json
    ├── home.json
    └── auth.json
```

```json
// public/locales/zh-CN/common.json
{
  "nav": {
    "home": "首页",
    "about": "关于我们",
    "contact": "联系我们"
  },
  "actions": {
    "save": "保存",
    "cancel": "取消",
    "delete": "删除",
    "confirm": "确认"
  },
  "messages": {
    "welcome": "欢迎, {{name}}!",
    "items_count": "{{count}} 个项目",
    "items_count_one": "{{count}} 个项目",
    "items_count_other": "{{count}} 个项目",
    "last_login": "上次登录: {{date, datetime}}",
    "price": "价格: {{amount, currency}}"
  }
}
```

```json
// public/locales/en/common.json
{
  "nav": {
    "home": "Home",
    "about": "About Us",
    "contact": "Contact"
  },
  "actions": {
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "confirm": "Confirm"
  },
  "messages": {
    "welcome": "Welcome, {{name}}!",
    "items_count": "{{count}} item",
    "items_count_one": "{{count}} item",
    "items_count_other": "{{count}} items",
    "last_login": "Last login: {{date, datetime}}",
    "price": "Price: {{amount, currency}}"
  }
}
```

### 2.3 在组件中使用

```tsx
import { useTranslation, Trans } from 'react-i18next';

function HomePage() {
  const { t, i18n } = useTranslation(['common', 'home']);

  return (
    <div>
      {/* 基本翻译 */}
      <h1>{t('nav.home')}</h1>

      {/* 带变量 */}
      <p>{t('messages.welcome', { name: '张三' })}</p>

      {/* 复数形式 */}
      <p>{t('messages.items_count', { count: 5 })}</p>
      <p>{t('messages.items_count', { count: 1 })}</p>

      {/* 使用其他命名空间 */}
      <p>{t('home:hero.title')}</p>

      {/* Trans 组件（支持内嵌 JSX） */}
      <Trans i18nKey="home:hero.description">
        阅读我们的 <a href="/docs">文档</a> 了解更多
      </Trans>

      {/* 语言切换 */}
      <select
        value={i18n.language}
        onChange={(e) => i18n.changeLanguage(e.target.value)}
      >
        <option value="zh-CN">中文</option>
        <option value="en">English</option>
        <option value="ja">日本語</option>
      </select>
    </div>
  );
}
```

### 2.4 复数形式

```json
// common.json
{
  "items": "没有项目",
  "items_one": "{{count}} 个项目",
  "items_other": "{{count}} 个项目"
}
```

```tsx
// 使用
t('items', { count: 0 })   // "没有项目"
t('items', { count: 1 })   // "1 个项目"
t('items', { count: 5 })   // "5 个项目"
```

### 2.5 格式化

```typescript
// src/i18n/index.ts 添加格式化
i18n.init({
  // ...
  interpolation: {
    escapeValue: false,
    format: (value, format, lng) => {
      if (format === 'datetime') {
        return new Intl.DateTimeFormat(lng).format(value);
      }
      if (format === 'currency') {
        const currency = lng === 'zh-CN' ? 'CNY' : 'USD';
        return new Intl.NumberFormat(lng, {
          style: 'currency',
          currency,
        }).format(value);
      }
      if (format === 'number') {
        return new Intl.NumberFormat(lng).format(value);
      }
      return value;
    },
  },
});
```

```tsx
// 使用格式化
t('messages.last_login', { date: new Date() })     // "上次登录: 2024/1/15"
t('messages.price', { amount: 99.9 })               // "价格: ¥99.90"
```

### 2.6 RTL 支持

```tsx
// RTL 语言：阿拉伯语、希伯来语等

// 检测 RTL
const rtlLanguages = ['ar', 'he', 'fa', 'ur'];
const isRTL = rtlLanguages.includes(i18n.language);

// 在根元素设置 dir
useEffect(() => {
  document.documentElement.dir = isRTL ? 'rtl' : 'ltr';
  document.documentElement.lang = i18n.language;
}, [i18n.language, isRTL]);

// CSS 逻辑属性（推荐使用）
.container {
  /* 不要用 left/right */
  margin-inline-start: 1rem;   /* LTR: left, RTL: right */
  margin-inline-end: 1rem;
  padding-inline: 1rem;
  border-inline-start: 2px solid blue;
  text-align: start;           /* LTR: left, RTL: right */
}

// Tailwind CSS RTL 支持
// tailwind.config.js
module.exports = {
  plugins: [
    require('tailwindcss-rtl'),
  ],
};
```

```tsx
// 使用 Tailwind RTL
<div className="ms-4 me-2 ps-3 pe-1 text-start">
  {/* ms = margin-start, me = margin-end */}
  {/* ps = padding-start, pe = padding-end */}
</div>
```

---

## 三、懒加载翻译

```typescript
// src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

i18n
  .use(Backend)  // 使用 HTTP backend 懒加载
  .use(initReactI18next)
  .init({
    backend: {
      loadPath: '/locales/{{lng}}/{{ns}}.json',
      // 或使用 API
      // loadPath: '/api/translations?lang={{lng}}&ns={{ns}}',
    },
    // 只加载默认语言
    preload: ['zh-CN'],
    // 其他语言按需加载
  });
```

---

## 四、无障碍 (Accessibility / a11y)

### 4.1 ARIA 属性

```tsx
// ARIA = Accessible Rich Internet Applications
// 为屏幕阅读器提供语义信息

// ===== ARIA 角色 =====
<div role="alert">表单提交失败</div>
<div role="status">3 条新消息</div>
<nav role="navigation">...</nav>
<div role="dialog" aria-modal="true">...</div>
<div role="tablist">
  <button role="tab" aria-selected={activeTab === 0}>标签1</button>
  <button role="tab" aria-selected={activeTab === 1}>标签2</button>
</div>

// ===== ARIA 属性 =====
// aria-label - 元素的可访问名称
<button aria-label="关闭" onClick={onClose}>
  <CloseIcon />  {/* 图标按钮没有文本，需要 aria-label */}
</button>

// aria-labelledby - 引用另一个元素作为标签
<h2 id="section-title">用户设置</h2>
<section aria-labelledby="section-title">...</section>

// aria-describedby - 引用描述元素
<input aria-describedby="email-hint" />
<p id="email-hint">我们会发送确认邮件到此地址</p>

// aria-expanded - 展开/收起状态
<button aria-expanded={isOpen} onClick={toggle}>
  菜单
</button>
{isOpen && <DropdownMenu />}

// aria-hidden - 从无障碍树中隐藏
<span aria-hidden="true">→</span>  {/* 装饰性元素 */}

// aria-live - 动态内容区域
<div aria-live="polite" aria-atomic="true">
  {message && <p>{message}</p>}
</div>

// aria-required / aria-invalid
<input aria-required="true" aria-invalid={hasError} />
```

### 4.2 语义化 HTML

```tsx
// ✅ 使用语义化元素
<header>      {/* 代替 <div className="header"> */}
  <nav>       {/* 代替 <div className="nav"> */}
    <ul>
      <li><a href="/">首页</a></li>
    </ul>
  </nav>
</header>

<main>        {/* 代替 <div className="main"> */}
  <article>   {/* 代替 <div className="article"> */}
    <header>
      <h1>标题</h1>
      <time dateTime="2024-01-15">2024年1月15日</time>
    </header>
    <section>内容</section>
    <aside>侧边栏</aside>
  </article>
</main>

<footer>      {/* 代替 <div className="footer"> */}
  <small>&copy; 2024</small>
</footer>

// ❌ 不好的做法
<div className="header">
  <div className="nav">
    <div className="nav-item" onClick={navigate}>首页</div>
  </div>
</div>

// ✅ 好的做法
<header>
  <nav>
    <ul>
      <li><Link to="/">首页</Link></li>
    </ul>
  </nav>
</header>
```

### 4.3 焦点管理

```tsx
import { useRef, useEffect } from 'react';

// ===== Modal 焦点陷阱 =====
function Modal({ isOpen, onClose, children }) {
  const modalRef = useRef<HTMLDivElement>(null);
  const previousFocusRef = useRef<HTMLElement | null>(null);

  useEffect(() => {
    if (isOpen) {
      // 保存当前焦点元素
      previousFocusRef.current = document.activeElement as HTMLElement;

      // 聚焦到 modal
      modalRef.current?.focus();

      // 监听 Tab 键
      const handleKeyDown = (e: KeyboardEvent) => {
        if (e.key === 'Escape') {
          onClose();
          return;
        }

        if (e.key !== 'Tab') return;

        const modal = modalRef.current;
        if (!modal) return;

        const focusables = modal.querySelectorAll<HTMLElement>(
          'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
        );

        const first = focusables[0];
        const last = focusables[focusables.length - 1];

        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        }
      };

      document.addEventListener('keydown', handleKeyDown);
      return () => document.removeEventListener('keydown', handleKeyDown);
    } else {
      // 恢复之前的焦点
      previousFocusRef.current?.focus();
    }
  }, [isOpen, onClose]);

  if (!isOpen) return null;

  return (
    <div
      ref={modalRef}
      role="dialog"
      aria-modal="true"
      tabIndex={-1}
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/50"
    >
      <div className="bg-white rounded-lg p-6">
        <button onClick={onClose} aria-label="关闭">X</button>
        {children}
      </div>
    </div>
  );
}

// ===== 跳过链接 =====
function SkipLink() {
  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4
                 focus:z-50 focus:bg-white focus:px-4 focus:py-2 focus:shadow-lg"
    >
      跳转到主要内容
    </a>
  );
}

function Layout({ children }) {
  return (
    <>
      <SkipLink />
      <header>...</header>
      <main id="main-content">{children}</main>
      <footer>...</footer>
    </>
  );
}
```

### 4.4 键盘导航

```tsx
// ===== Tab 管理 =====
function Tabs({ tabs, children }) {
  const [activeIndex, setActiveIndex] = useState(0);
  const tabRefs = useRef<(HTMLButtonElement | null)[]>([]);

  const handleKeyDown = (e: KeyboardEvent, index: number) => {
    let newIndex = index;

    switch (e.key) {
      case 'ArrowRight':
        newIndex = (index + 1) % tabs.length;
        break;
      case 'ArrowLeft':
        newIndex = (index - 1 + tabs.length) % tabs.length;
        break;
      case 'Home':
        newIndex = 0;
        break;
      case 'End':
        newIndex = tabs.length - 1;
        break;
      default:
        return;
    }

    e.preventDefault();
    setActiveIndex(newIndex);
    tabRefs.current[newIndex]?.focus();
  };

  return (
    <div>
      <div role="tablist" aria-label="标签页">
        {tabs.map((tab, index) => (
          <button
            key={tab}
            ref={el => { tabRefs.current[index] = el; }}
            role="tab"
            aria-selected={index === activeIndex}
            aria-controls={`panel-${index}`}
            id={`tab-${index}`}
            tabIndex={index === activeIndex ? 0 : -1}
            onClick={() => setActiveIndex(index)}
            onKeyDown={(e) => handleKeyDown(e, index)}
          >
            {tab}
          </button>
        ))}
      </div>
      <div
        role="tabpanel"
        id={`panel-${activeIndex}`}
        aria-labelledby={`tab-${activeIndex}`}
        tabIndex={0}
      >
        {children[activeIndex]}
      </div>
    </div>
  );
}
```

### 4.5 表单无障碍

```tsx
function AccessibleForm() {
  const [errors, setErrors] = useState<Record<string, string>>({});

  return (
    <form
      aria-label="注册表单"
      onSubmit={handleSubmit}
      noValidate  /* 使用自定义验证 */
    >
      {/* 用 fieldset 分组 */}
      <fieldset>
        <legend>个人信息</legend>

        <div>
          <label htmlFor="name">
            姓名 <span aria-hidden="true">*</span>
          </label>
          <input
            id="name"
            type="text"
            required
            aria-required="true"
            aria-invalid={!!errors.name}
            aria-describedby={errors.name ? 'name-error' : undefined}
          />
          {errors.name && (
            <p id="name-error" role="alert" className="text-red-500">
              {errors.name}
            </p>
          )}
        </div>

        <div>
          <label htmlFor="email">
            邮箱 <span aria-hidden="true">*</span>
          </label>
          <input
            id="email"
            type="email"
            required
            aria-required="true"
            aria-invalid={!!errors.email}
            aria-describedby="email-hint email-error"
          />
          <p id="email-hint" className="text-sm text-gray-500">
            我们不会分享您的邮箱
          </p>
          {errors.email && (
            <p id="email-error" role="alert" className="text-red-500">
              {errors.email}
            </p>
          )}
        </div>
      </fieldset>

      {/* 错误摘要 */}
      {Object.keys(errors).length > 0 && (
        <div role="alert" aria-live="assertive" className="error-summary">
          <h2>表单有 {Object.keys(errors).length} 个错误</h2>
          <ul>
            {Object.entries(errors).map(([field, message]) => (
              <li key={field}>
                <a href={`#${field}`}>{message}</a>
              </li>
            ))}
          </ul>
        </div>
      )}

      <button type="submit">注册</button>
    </form>
  );
}
```

### 4.6 屏幕阅读器测试

```
常用屏幕阅读器：
- macOS: VoiceOver (内置)
- Windows: NVDA (免费) / JAWS (付费)
- Linux: Orca
- 移动端: VoiceOver (iOS) / TalkBack (Android)

测试检查清单：
□ 所有图片有 alt 属性
□ 表单元素有关联的 label
□ 按钮和链接有描述性文本
□ 标题层级正确 (h1 > h2 > h3)
□ 动态内容有 aria-live
□ 焦点顺序合理
□ 键盘可以完成所有操作
□ 颜色不是唯一的信息传达方式
□ 文本对比度足够 (4.5:1)
```

### 4.7 自动化测试

```bash
npm install -D @axe-core/react axe-core
```

```tsx
// 自动检测无障碍问题
// src/setupTests.ts
import 'jest-axe/extend-expect';
import { axe, toHaveNoViolations } from 'jest-axe';

expect.extend(toHaveNoViolations);

// 在测试中使用
import { render } from '@testing-library/react';
import { axe } from 'jest-axe';

it('should have no accessibility violations', async () => {
  const { container } = render(<MyComponent />);
  const results = await axe(container);
  expect(results).toHaveNoViolations();
});

// ESLint 插件
// eslint-plugin-jsx-a11y
rules: {
  'jsx-a11y/alt-text': 'error',
  'jsx-a11y/anchor-has-content': 'error',
  'jsx-a11y/aria-props': 'error',
  'jsx-a11y/aria-role': 'error',
  'jsx-a11y/click-events-have-key-events': 'warn',
  'jsx-a11y/heading-has-content': 'error',
  'jsx-a11y/label-has-associated-control': 'error',
  'jsx-a11y/no-autofocus': 'warn',
  'jsx-a11y/no-distracting-elements': 'error',
}
```

---

## 五、综合实践

### 5.1 国际化 + 无障碍结合

```tsx
// 支持 RTL 和屏幕阅读器的语言切换器
function LanguageSwitcher() {
  const { i18n, t } = useTranslation();

  const handleChange = (lng: string) => {
    i18n.changeLanguage(lng);
    // 更新页面方向
    document.documentElement.dir = ['ar', 'he'].includes(lng) ? 'rtl' : 'ltr';
    document.documentElement.lang = lng;
  };

  return (
    <div role="group" aria-label={t('accessibility.language')}>
      <select
        value={i18n.language}
        onChange={(e) => handleChange(e.target.value)}
        aria-label={t('accessibility.select_language')}
      >
        <option value="zh-CN">中文（简体）</option>
        <option value="en">English</option>
        <option value="ja">日本語</option>
        <option value="ar">العربية</option>
      </select>
    </div>
  );
}
```

---

## 总结

| 概念 | 要点 |
|------|------|
| **react-i18next** | 流行的 React 国际化方案，支持命名空间和懒加载 |
| **复数/格式化** | ICU MessageFormat，内置日期/数字/货币格式化 |
| **RTL 支持** | 使用 CSS 逻辑属性（inline-start/end） |
| **ARIA** | 为屏幕阅读器提供语义信息 |
| **语义化 HTML** | 用正确的元素（nav/main/article）代替 div |
| **焦点管理** | Modal 焦点陷阱、跳过链接、键盘导航 |
| **自动化测试** | jest-axe 自动检测无障碍违规 |
