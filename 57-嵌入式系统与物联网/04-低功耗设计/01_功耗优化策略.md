# 功耗优化策略


## 嵌入式系统功耗优化策略

一、功耗概述

## 一、功耗基础


低功耗设计是嵌入式系统和物联网设备的关键需求，直接影响设备的续航能力和应用场景。


### 1.1 功耗组成


| 类型 | 公式 | 说明 |
| --- | --- | --- |
| 动态功耗 | `P = C × V² × f` | 电容充放电功耗，与频率和电压相关 |
| 静态功耗 | `P = V × I_leak` | 漏电流功耗，与温度和工艺相关 |
| 短路功耗 | 与转换时间相关 | 信号转换期间的短路电流 |

**动态功耗公式 P = C × V² × f：**


C = 负载电容，V = 供电电压，f = 时钟频率


降低电压是最有效的节能手段（平方关系）。
二、睡眠模式

## 二、睡眠模式（Sleep Modes）


微控制器通过进入不同的低功耗模式来减少能耗，唤醒时间与功耗成反比。


### 2.1 STM32低功耗模式


| 模式 | CPU | 时钟 | SRAM | 唤醒时间 | 功耗 |
| --- | --- | --- | --- | --- | --- |
| Sleep | 停止 | 运行 | 保持 | 立即 | 中等 |
| Stop | 停止 | 停止 | 保持 | 微秒级 | 低 |
| Standby | 停止 | 停止 | 丢失 | 毫秒级 | 极低 |


### 2.2 ESP32功耗模式


| 模式 | CPU | WiFi | 功耗 |
| --- | --- | --- | --- |
| Active | 运行 | 开启 | ~240mA |
| Modem Sleep | 运行 | 关闭 | ~30mA |
| Light Sleep | 暂停 | 关闭 | ~800μA |
| Deep Sleep | 关闭 | 关闭 | ~10μA |

三、时钟门控

## 三、时钟门控（Clock Gating）


时钟门控通过在不需要时关闭模块的时钟来减少动态功耗。


### 3.1 实现层次


| 层次 | 说明 | 粒度 |
| --- | --- | --- |
| 系统级 | 关闭整个外设模块的时钟 | 最粗 |
| 模块级 | 关闭子模块的时钟 | 中等 |
| 寄存器级 | 仅在需要时更新寄存器 | 最细 |


> **Note:** **STM32示例：**
> 通过RCC寄存器关闭未使用的外设时钟。
>
>
> `RCC->APB1ENR &= ~RCC_APB1ENR_TIM2EN;`
> // 关闭TIM2时钟

四、动态电压频率调节

## 四、DVFS（动态电压频率调节）


DVFS根据处理需求动态调整CPU的电压和频率，在满足性能需求的同时最小化功耗。


### 4.1 工作原理


- 当CPU负载低时，降低频率和电压
- 当需要高性能时，提升频率和电压
- 电压与频率需成比例调整

**节能效果：**


功耗 ∝ V² × f，若电压和频率同时降低50%，功耗降至原来的12.5%（0.5² × 0.5 = 0.125）。

### 4.2 实现方式


| 方式 | 说明 |
| --- | --- |
| 基于负载 | 监测CPU利用率，动态调节 |
| 基于预测 | 根据历史数据预测负载变化 |
| 基于温度 | 温度过高时降频保护 |

五、占空比调制

## 五、占空比调制（Duty Cycling）


通过让设备周期性地在活跃和休眠之间切换，大幅降低平均功耗。


### 5.1 典型工作周期


> **Note:** **示例：传感器节点**
>
>
> - 活跃时间：10ms（采集数据+发送）
>
>
> - 休眠时间：9990ms
>
>
> - 占空比：0.1%
>
>
> - 如果活跃功耗10mA，休眠功耗10μA，则平均功耗 ≈ 20μA


### 5.2 实现策略


- **时间驱动：**
   按固定周期唤醒
- **事件驱动：**
   外部事件（中断）唤醒
- **混合驱动：**
   定时唤醒 + 事件唤醒

六、其他优化策略

## 六、其他功耗优化策略


### 6.1 硬件层面


- **选择低功耗芯片：**
   选用合适的MCU和外围器件
- **电源管理IC：**
   使用高效DC-DC转换器
- **IO配置：**
   未用引脚配置为输出低电平，避免浮空
- **外围电路：**
   使用低功耗传感器和通信模块


### 6.2 软件层面


- **中断驱动：**
   避免轮询，使用中断响应事件
- **DMA传输：**
   减少CPU参与数据搬运
- **高效算法：**
   减少计算量和内存访问
- **代码优化：**
   使用查找表代替复杂计算
- **减少唤醒次数：**
   合并任务，批量处理


### 6.3 通信优化


- **减少传输频率：**
   数据压缩后再发送
- **选择合适的协议：**
   MQTT/CoAP比HTTP更省电
- **数据聚合：**
   多个数据合并为一次传输

========================================
  文件总结
========================================
  主题：嵌入式系统功耗优化策略
  内容概要：
    1. 功耗基础 - 动态功耗(P=C×V²×f)、静态功耗、短路功耗
    2. 睡眠模式 - STM32(Sleep/Stop/Standby)、ESP32(Active/Modem/Light/Deep)
    3. 时钟门控 - 系统/模块/寄存器级时钟控制
    4. DVFS - 动态电压频率调节，功耗与V²×f成正比
    5. 占空比调制 - 活跃/休眠周期切换，降低平均功耗
  重点知识：
    - 动态功耗公式：P = C × V² × f，降压是最有效的节能手段
    - 电压和频率同时降50%，功耗降至12.5%
    - 选择合适的睡眠模式平衡唤醒延迟和功耗
    - 占空比调制通过降低活跃时间比例来节能
========================================


<!-- Converted from: 01_功耗优化策略.html -->
