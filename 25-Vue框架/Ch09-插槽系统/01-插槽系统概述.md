# 插槽系统概述

## 一、概念说明
**插槽（Slots）**是 Vue 的**内容分发机制**，允许父组件向子组件的模板中插入内容。插槽让组件变得更加灵活和可复用。

## 二、插槽类型

### 2.1 三种插槽
```
默认插槽    → 子组件 <slot> 中的默认内容区域
具名插槽    → 通过 name 属性区分多个插槽位置
作用域插槽  → 子组件向父组件传递数据
```

### 2.2 基本示例
```vue
<!-- 子组件 Card.vue -->
<template>
  <div class="card">
    <header><slot name="header" /></header>
    <main><slot /></main>
    <footer><slot name="footer" /></footer>
  </div>
</template>
```

```vue
<!-- 父组件使用 -->
<template>
  <Card>
    <template #header>标题</template>
    <p>主内容</p>
    <template #footer>页脚</template>
  </Card>
</template>
```

### 2.3 作用域插槽
```vue
<!-- 子组件传数据给父组件 -->
<template>
  <ul>
    <li v-for="item in items" :key="item.id">
      <slot :item="item" :index="index" />
    </li>
  </ul>
</template>
```

```vue
<!-- 父组件接收数据 -->
<template>
  <List :items="list">
    <template #default="{ item, index }">
      {{ index }}. {{ item.name }}
    </template>
  </List>
</template>
```

### 2.4 使用场景
- **布局组件**：header/content/footer 分区
- **列表组件**：自定义每一项的渲染
- **模态框**：自定义标题、内容、按钮
- **高阶组件**：控制渲染逻辑的封装

## 三、注意事项与常见陷阱
- 默认插槽的内容在 `<slot>` 标签的后备内容位置显示
- 父组件的模板内容会在编译时替换子组件的插槽
- 作用域插槽使子组件成为"渲染器"
- 缩写 `#` 只能用在 `<template>` 上
