# 服务端渲染概念

## 一、概念说明

**服务端渲染**（Server-Side Rendering，SSR）指在服务器端将 Vue 组件渲染为 HTML 字符串，直接发送给浏览器。相比客户端渲染（CSR），SSR 能提供更快的首屏加载和更好的 SEO。

SSR 的流程：服务器接收请求 → 运行 Vue 渲染组件为 HTML → 发送完整 HTML 到浏览器 → 浏览器显示内容 → 客户端"激活"（hydration）使页面可交互。

```vue
<script setup>
// SSR 中每个请求都需要创建新的应用实例
// 不能使用浏览器特有的 API（window、document）
import { ref } from 'vue'

const message = ref('这段内容在服务器端就渲染好了')
</script>

<template>
  <div>
    <h1>{{ message }}</h1>
  </div>
</template>
```

## 二、具体用法

### 2.1 Nuxt.js 框架

Nuxt 是 Vue 生态中最成熟的 SSR 框架，开箱即用：

```bash
pnpm dlx nuxi@latest init my-nuxt-app
```

```vue
<!-- pages/index.vue - Nuxt 自动路由 -->
<script setup>
const { data } = await useFetch('/api/posts')
</script>

<template>
  <div>
    <h1>文章列表</h1>
    <article v-for="post in data" :key="post.id">
      <h2>{{ post.title }}</h2>
      <p>{{ post.summary }}</p>
    </article>
  </div>
</template>
```

### 2.2 CSR vs SSR vs SSG

| 渲染方式 | 全称 | 适用场景 |
|---------|------|---------|
| CSR | Client-Side Rendering | 后台管理系统 |
| SSR | Server-Side Rendering | 需要 SEO 的动态页面 |
| SSG | Static Site Generation | 博客、文档站 |

## 三、注意事项与常见陷阱

- SSR 代码不能使用浏览器 API（`window`、`document`、`localStorage`）
- 第三方库可能不兼容 SSR（需动态导入或条件判断）
- SSR 增加服务器负载和响应时间
- 需要处理数据预取和状态序列化
- hydration 不匹配会导致页面闪烁或交互异常
