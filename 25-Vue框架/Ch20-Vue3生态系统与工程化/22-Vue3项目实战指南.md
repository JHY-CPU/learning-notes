# Vue3项目实战指南

## 一、概念说明

从零搭建一个 Vue 3 生产级项目需要覆盖技术选型、项目初始化、目录规范、开发配置、测试、CI/CD、部署等全流程。本指南按实际项目经验整理每个步骤的最佳实践。

## 二、具体用法

### 第一步：项目初始化

```bash
# 创建项目
npm create vite@latest my-app -- --template vue-ts
cd my-app
npm install

# 安装核心依赖
npm install vue-router@4 pinia axios
npm install -D @vueuse/core

# 安装 UI 库（按需选择）
npm install element-plus @element-plus/icons-vue

# 安装开发工具
npm install -D eslint prettier @vue/eslint-config-typescript
npm install -D vitest @vue/test-utils jsdom
npm install -D sass
```

### 第二步：目录结构

```
my-app/
├── src/
│   ├── assets/           # 静态资源
│   │   ├── styles/       # 全局样式
│   │   └── images/
│   ├── components/       # 公共组件
│   │   └── common/       # 通用基础组件
│   ├── composables/      # 组合式函数
│   ├── layouts/          # 布局组件
│   ├── router/           # 路由配置
│   │   ├── index.ts
│   │   ├── modules/      # 路由模块
│   │   └── guards.ts     # 路由守卫
│   ├── stores/           # Pinia stores
│   ├── types/            # TypeScript 类型
│   ├── utils/            # 工具函数
│   ├── views/            # 页面组件
│   ├── App.vue
│   ├── main.ts
│   └── env.d.ts
├── public/
├── .env.development
├── .env.production
├── vite.config.ts
├── tsconfig.json
├── eslint.config.js
└── package.json
```

### 第三步：核心配置

```ts
// vite.config.ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      imports: ['vue', 'vue-router', 'pinia', '@vueuse/core'],
      resolvers: [ElementPlusResolver()]
    }),
    Components({ resolvers: [ElementPlusResolver()] })
  ],
  resolve: {
    alias: { '@': resolve(__dirname, 'src') }
  },
  css: {
    preprocessorOptions: {
      scss: { additionalData: `@use "@/assets/styles/variables" as *;` }
    }
  }
})
```

```ts
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import { setupPermissionGuard } from './guards'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      component: () => import('@/layouts/DefaultLayout.vue'),
      children: [
        { path: '', name: 'home', component: () => import('@/views/HomeView.vue') }
      ]
    },
    {
      path: '/login',
      name: 'login',
      component: () => import('@/views/LoginView.vue'),
      meta: { public: true }
    }
  ]
})

setupPermissionGuard(router)
export default router
```

### 第四步：测试配置

```ts
// vitest.config.ts
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  test: {
    environment: 'jsdom',
    globals: true,
    include: ['src/**/*.{test,spec}.ts']
  }
})
```

```ts
// src/components/MyButton.test.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import MyButton from './MyButton.vue'

describe('MyButton', () => {
  it('renders correctly', () => {
    const wrapper = mount(MyButton, { props: { label: '点击' } })
    expect(wrapper.text()).toContain('点击')
  })

  it('emits click event', async () => {
    const wrapper = mount(MyButton, { props: { label: '点击' } })
    await wrapper.trigger('click')
    expect(wrapper.emitted('click')).toHaveLength(1)
  })
})
```

### 第五步：部署配置

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 18, cache: npm }
      - run: npm ci
      - run: npm run lint
      - run: npm run type-check
      - run: npm run test
      - run: npm run build
```

### 项目脚本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint . --fix",
    "type-check": "vue-tsc --noEmit",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
```

## 三、注意事项与常见陷阱

1. **先规划再编码**：目录结构和命名规范在项目初期确定，后期调整成本高
2. **自动导入让代码简洁但降低可读性**：团队新成员可能不知道 ref 来自哪里
3. **TypeScript 严格模式从一开始开启**：后期开启会导致大量类型错误需要修复
4. **测试覆盖率不追求 100%**：核心逻辑和公共组件优先覆盖
5. **定期更新依赖**：使用 `npm outdated` 检查过期依赖，`ncu` 批量更新
