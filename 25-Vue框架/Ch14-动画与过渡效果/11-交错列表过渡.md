# 交错列表过渡

## 一、概念说明

交错列表过渡是指列表中每个元素的动画**依次延迟执行**，产生波浪式或级联式的视觉效果。这需要结合 JavaScript 钩子来动态设置每个元素的延迟时间。

```vue
<script setup>
import { ref } from 'vue'
const items = ref([])
let nextId = 0

function addItem() {
  items.value.push({ id: nextId++, text: `项目 ${nextId}` })
}
function onBeforeEnter(el) {
  el.style.opacity = 0
  el.style.transform = 'translateY(20px)'
}
function onEnter(el, done) {
  const delay = el.dataset.index * 100
  setTimeout(() => {
    el.style.transition = 'all 0.3s ease'
    el.style.opacity = 1
    el.style.transform = 'translateY(0)'
    el.addEventListener('transitionend', done)
  }, delay)
}
</script>

<template>
  <button @click="addItem">添加项目</button>
  <TransitionGroup
    tag="ul"
    @before-enter="onBeforeEnter"
    @enter="onEnter"
  >
    <li v-for="(item, index) in items" :key="item.id" :data-index="index">
      {{ item.text }}
    </li>
  </TransitionGroup>
</template>
```

## 二、具体用法

### 2.1 使用 data 属性传递索引

通过 `:data-index` 将元素在列表中的索引传递给 JavaScript 钩子，用于计算延迟。

### 2.2 动态延迟计算

```js
function onEnter(el, done) {
  const delay = el.dataset.index * 100 // 每个元素延迟 100ms
  setTimeout(() => {
    // 执行动画后调用 done
    done()
  }, delay)
}
```

## 三、注意事项与常见陷阱

- 必须调用 `done` 回调，否则 Vue 不知道动画何时结束
- 使用 `data-*` 属性传递数据给钩子函数
- 延迟时间不宜过长，否则列表较长时最后一个元素等待过久
