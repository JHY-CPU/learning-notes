# 观察者模式

## 一、概念说明

观察者模式定义对象间一对多的依赖关系。TypeScript 通过泛型确保事件类型和回调参数的类型安全。

## 二、具体用法

### 2.1 泛型事件系统

```typescript
type EventMap = {
  login: (userId: number) => void;
  logout: () => void;
  error: (error: Error) => void;
};

class TypedEventEmitter<TEvents extends Record<string, (...args: any[]) => void>> {
  private listeners = new Map<keyof TEvents, Set<Function>>();

  on<K extends keyof TEvents>(event: K, listener: TEvents[K]): () => void {
    if (!this.listeners.has(event)) {
      this.listeners.set(event, new Set());
    }
    this.listeners.get(event)!.add(listener);

    // 返回取消订阅函数
    return () => this.off(event, listener);
  }

  off<K extends keyof TEvents>(event: K, listener: TEvents[K]): void {
    this.listeners.get(event)?.delete(listener);
  }

  emit<K extends keyof TEvents>(event: K, ...args: Parameters<TEvents[K]>): void {
    this.listeners.get(event)?.forEach(fn => fn(...args));
  }
}

// 使用
const emitter = new TypedEventEmitter<EventMap>();
const unsubscribe = emitter.on('login', (userId) => {
  console.log(userId); // number
});
emitter.emit('login', 123); // 类型安全
unsubscribe();
```

### 2.2 发布订阅模式

```typescript
interface Subscriber<T> {
  update(data: T): void;
}

class Publisher<T> {
  private subscribers = new Set<Subscriber<T>>();

  subscribe(subscriber: Subscriber<T>): () => void {
    this.subscribers.add(subscriber);
    return () => this.subscribers.delete(subscriber);
  }

  publish(data: T): void {
    this.subscribers.forEach(sub => sub.update(data));
  }
}
```

## 三、注意事项与常见陷阱

1. **使用泛型约束事件参数**：确保 `emit` 和 `on` 类型匹配
2. **返回取消订阅函数**：防止内存泄漏
3. **`Set` 存储监听器**：自动去重
4. **事件名用字面量联合类型**：IDE 自动补全
