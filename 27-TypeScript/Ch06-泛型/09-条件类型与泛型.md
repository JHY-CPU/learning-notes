# 条件类型与泛型

## 一、概念说明

条件类型结合泛型，可以根据类型参数的关系选择不同的类型结果。语法 `T extends U ? X : Y` 在泛型中实现了类型级别的"条件分支"。

## 二、具体用法

### 2.1 基本条件类型

```typescript
type IsString<T> = T extends string ? "yes" : "no";

type A = IsString<"hello">; // "yes"
type B = IsString<42>;      // "no"

console.log("条件类型在编译时计算");
// 输出: 条件类型在编译时计算
```

### 2.2 配合 infer

```typescript
type UnpackPromise<T> = T extends Promise<infer U> ? U : T;

type R1 = UnpackPromise<Promise<string>>; // string
type R2 = UnpackPromise<number>;           // number
```

### 2.3 分布式条件类型

```typescript
type ToArray<T> = T extends unknown ? T[] : never;
type Result = ToArray<string | number>; // string[] | number[]
// 联合类型逐个分发
```

## 三、注意事项与常见陷阱

1. **分布式特性**：联合类型会被逐个分发处理
2. **`infer` 只能用在条件类型中**
3. **`never` 的特殊行为**：`never extends T ? A : B` 结果为 `never`
4. **条件类型可嵌套**：但过多影响可读性
