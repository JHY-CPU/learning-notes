# 泛型类

## 一、概念说明

泛型类在类定义时引入类型参数，使类的属性、方法可以使用泛型类型。实例化时指定具体类型，整个类实例的类型随之确定。

## 二、具体用法

### 2.1 基本泛型类

```typescript
class Stack<T> {
  private items: T[] = [];

  push(item: T): void {
    this.items.push(item);
  }

  pop(): T | undefined {
    return this.items.pop();
  }

  peek(): T | undefined {
    return this.items[this.items.length - 1];
  }
}

const numStack = new Stack<number>();
numStack.push(1);
numStack.push(2);
console.log(numStack.pop()); // 输出: 2

const strStack = new Stack<string>();
strStack.push("hello");
console.log(strStack.peek()); // 输出: hello
```

### 2.2 泛型类约束

```typescript
class SortableList<T extends { toString(): string }> {
  private items: T[] = [];

  add(item: T): void {
    this.items.push(item);
  }

  sorted(): T[] {
    return [...this.items].sort((a, b) =>
      a.toString().localeCompare(b.toString())
    );
  }
}

const list = new SortableList<string>();
list.add("banana");
list.add("apple");
list.add("cherry");
console.log(list.sorted());
// 输出: ["apple", "banana", "cherry"]
```

## 三、注意事项与常见陷阱

1. **静态成员不能用泛型**：`static item: T` 不合法
2. **实例化时指定类型**：`new Stack<number>()`
3. **类型参数在整个类中共享**：属性和方法共用同一个 `T`
4. **泛型类的继承**：子类可以指定具体类型或继续泛型
