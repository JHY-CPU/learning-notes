# 三斜线指令

## 一、概念说明

三斜线指令（Triple-Slash Directives）以 `///` 开头，用于声明文件间的依赖关系。在现代 TypeScript 项目中已基本被 ES Module 的 `import` 取代，仅在极少数场景使用。

## 二、具体用法

### 2.1 引用类型声明

```typescript
/// <reference types="node" />
// 等价于安装 @types/node，现代项目直接用 npm install -D @types/node

/// <reference path="./types/global.d.ts" />
// 引用相对路径的类型声明文件
```

### 2.2 引用其他声明文件

```typescript
/// <reference path="jquery.d.ts" />
// 已过时，现代项目使用 import
```

## 三、注意事项与常见陷阱

1. **已过时**：现代项目使用 ES Module `import`
2. **`types` 属性**：引用 `@types` 包
3. **`path` 属性**：引用相对路径声明文件
4. **仅在 `.d.ts` 文件中有意义**：普通 `.ts` 文件中不需要
