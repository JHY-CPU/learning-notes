# ES 模块导入

## 一、概念说明

ES Module 使用 `import` 导入模块成员，支持命名导入、默认导入、命名空间导入和动态导入。

## 二、具体用法

### 2.1 命名导入

```typescript
// 导入指定成员
import { add, multiply } from "./math.js";

console.log(add(2, 3));      // 输出: 5
console.log(multiply(4, 5)); // 输出: 20

// 重命名导入
import { add as sum } from "./math.js";
console.log(sum(1, 2)); // 输出: 3
```

### 2.2 默认导入

```typescript
// 导入默认导出（名称自定）
import UserService from "./user-service.js";

const service = new UserService();
console.log(service.getUsers());
// 输出: ["Alice", "Bob"]
```

### 2.3 命名空间导入

```typescript
// 导入所有成员到一个命名空间
import * as MathUtils from "./math.js";

console.log(MathUtils.add(1, 2));      // 输出: 3
console.log(MathUtils.multiply(3, 4)); // 输出: 12
```

### 2.4 混合导入

```typescript
import DefaultService, { helper, VERSION } from "./module.js";
```

## 三、注意事项与常见陷阱

1. **`.js` 扩展名**：TypeScript 文件导入需写 `.js`
2. **命名导入必须匹配**：名称需与导出一致
3. **默认导入名称自定**：`import Anything from "./mod"`
4. **导入的是只读引用**：不能修改导入的变量
