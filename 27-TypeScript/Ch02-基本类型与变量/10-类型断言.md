# 类型断言

## 一、概念说明

类型断言（Type Assertion）告诉编译器"我知道这个值的类型比你推断的更具体"。有两种语法：`as 类型` 和尖括号 `<类型>`。类型断言不改变运行时值，只影响编译时类型检查。

## 二、具体用法

### 2.1 as 语法

```typescript
// 从宽泛类型断言为具体类型
const someValue: unknown = "Hello, TypeScript";
const str: string = someValue as string;

console.log(str.toUpperCase());
// 输出: HELLO, TYPESCRIPT
```

### 2.2 DOM 元素断言

```typescript
// getElementById 返回 HTMLElement | null
const input = document.getElementById("username") as HTMLInputElement;

// 现在可以访问 input 特有的属性
input.value = "默认值";
console.log(input.value);
// 输出: 默认值
```

### 2.3 非空断言操作符 `!`

```typescript
// ! 告诉编译器"这个值不是 null 或 undefined"
function processUser(name: string | undefined) {
  // 非空断言：跳过 undefined 检查
  const upper = name!.toUpperCase();
  console.log(upper);
}

// ⚠️ 如果 name 是 undefined，运行时会崩溃
processUser("alice");
// 输出: ALICE
```

### 2.4 双重断言

```typescript
// 当 as 直接断言不兼容时，先断言为 unknown 再断言为目标类型
const num = 42;
const str2 = num as unknown as string; // 一般应避免

console.log(typeof str2); // 运行时仍是 number
// 输出: number
```

## 三、注意事项与常见陷阱

1. **类型断言不是转换**：`42 as string` 不会将数字变成字符串
2. **避免非空断言**：`!` 会跳过安全检查，尽量用类型守卫替代
3. **双重断言是危险信号**：说明类型设计有问题，应重新审视
4. **`const 断言` vs `类型断言`**：`as const` 是字面量断言，不同于类型断言
