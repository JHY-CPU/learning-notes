# 前端埋点与日志


## 前端埋点与日志


手动埋点/无痕埋点、性能数据采集、pv/uv 统计、上报策略(Beacon)。


## 埋点 API


```
// ========== 手动埋点 ==========
function track(event, data) {
    navigator.sendBeacon('/api/track', JSON.stringify({
        event, data,
        time: Date.now(),
        url: location.href,
        userAgent: navigator.userAgent
    }));
}

// ========== 无痕埋点 (自动) ==========
document.addEventListener('click', (e) => {
    const path = getEventPath(e.target);
    track('click', { path, text: e.target.textContent?.slice(0, 20) });
});

// ========== PV/UV 统计 ==========
// PV (Page View): 页面访问次数
// UV (Unique Visitor): 独立访客数
// 页面加载时上报: track('pv', { url: location.href, referrer });

// ========== 性能数据 ==========
const observer = new PerformanceObserver((list) => {
    for (const entry of list.getEntries()) {
        track('performance', { name: entry.name, value: entry.startTime });
    }
});
observer.observe({ entryTypes: ['paint', 'largest-contentful-paint'] });
```


## 演示：埋点与日志

点击按钮查看


<!-- Converted from: 80_前端埋点与日志.html -->
