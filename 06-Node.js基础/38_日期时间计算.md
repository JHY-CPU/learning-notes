# 日期时间计算


## 日期时间计算


日期差计算、工作日判断、时区转换、相对时间、日历生成。


## 日期计算方法


```
// ========== 日期差计算 ==========
function daysDiff(d1, d2) {
    const msDiff = Math.abs(d2 - d1);
    return Math.floor(msDiff / (1000 * 60 * 60 * 24));
}

// ========== 工作日判断 ==========
function isWeekday(date) {
    const day = date.getDay();
    return day !== 0 && day !== 6;
}

// ========== 添加天数 ==========
function addDays(date, days) {
    const result = new Date(date);
    result.setDate(result.getDate() + days);
    return result;
}

// ========== 相对时间 ==========
function timeAgo(date) {
    const diff = Date.now() - date;
    const units = [
        { name: '年', ms: 365 * 24 * 60 * 60 * 1000 },
        { name: '月', ms: 30 * 24 * 60 * 60 * 1000 },
        { name: '天', ms: 24 * 60 * 60 * 1000 },
        { name: '小时', ms: 60 * 60 * 1000 },
        { name: '分钟', ms: 60 * 1000 },
    ];
    for (const unit of units) {
        if (diff >= unit.ms) {
            const count = Math.floor(diff / unit.ms);
            return `${count} ${unit.name}前`;
        }
    }
    return '刚刚';
}
```


## 演示：日期时间计算

点击按钮查看


## Intl 与时区处理

```javascript
// ========== Intl.DateTimeFormat 时区 ==========
function formatInTimezone(date, timeZone, locale = 'zh-CN') {
    return new Intl.DateTimeFormat(locale, {
        timeZone,
        year: 'numeric',
        month: '2-digit',
        day: '2-digit',
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit',
        hour12: false,
    }).format(date);
}

const now = new Date();
console.log(formatInTimezone(now, 'Asia/Shanghai'));
// '2024/01/15 14:30:00'
console.log(formatInTimezone(now, 'America/New_York'));
// '2024/01/15 01:30:00'

// ========== 时区转换 ==========
function convertTimezone(date, fromTz, toTz) {
    const fromOffset = getTimezoneOffset(date, fromTz);
    const toOffset = getTimezoneOffset(date, toTz);
    const diff = toOffset - fromOffset;
    return new Date(date.getTime() + diff);
}

function getTimezoneOffset(date, timeZone) {
    const utcDate = new Date(date.toLocaleString('en-US', { timeZone: 'UTC' }));
    const tzDate = new Date(date.toLocaleString('en-US', { timeZone }));
    return tzDate - utcDate;
}
```

## 日历与日期工具

```javascript
// ========== 生成月历 ==========
function generateCalendar(year, month) {
    const firstDay = new Date(year, month - 1, 1).getDay(); // 0=周日
    const daysInMonth = new Date(year, month, 0).getDate();

    const calendar = [];
    let week = new Array(7).fill(null);

    for (let day = 1; day <= daysInMonth; day++) {
        const dayOfWeek = (firstDay + day - 1) % 7;
        week[dayOfWeek] = day;
        if (dayOfWeek === 6 || day === daysInMonth) {
            calendar.push(week);
            week = new Array(7).fill(null);
        }
    }
    return calendar;
}

// ========== 获取某月所有工作日 ==========
function getWorkdays(year, month) {
    const days = [];
    const daysInMonth = new Date(year, month, 0).getDate();
    for (let d = 1; d <= daysInMonth; d++) {
        const date = new Date(year, month - 1, d);
        if (isWeekday(date)) days.push(date);
    }
    return days;
}

// ========== 日期范围迭代 ==========
function* dateRange(startDate, endDate, step = 1) {
    const current = new Date(startDate);
    while (current <= endDate) {
        yield new Date(current);
        current.setDate(current.getDate() + step);
    }
}

// 使用
for (const date of dateRange(new Date('2024-01-01'), new Date('2024-01-07'))) {
    console.log(date.toISOString().split('T')[0]);
}
// 2024-01-01, 2024-01-02, ..., 2024-01-07

// ========== 计算年龄 ==========
function calculateAge(birthDate) {
    const today = new Date();
    let age = today.getFullYear() - birthDate.getFullYear();
    const monthDiff = today.getMonth() - birthDate.getMonth();
    if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birthDate.getDate())) {
        age--;
    }
    return age;
}

// ========== 节假日判断 (简化版) ==========
function isHoliday(date, holidays = []) {
    const dateStr = date.toISOString().split('T')[0];
    return holidays.includes(dateStr) || !isWeekday(date);
}
```

## Date 常见陷阱

```javascript
// ========== Date 陷阱 ==========
// 陷阱1: 月份从0开始
new Date(2024, 0, 1); // 2024年1月1日 (0=1月)

// 陷阱2: 日期解析不一致
new Date('2024-01-01'); // UTC 时间
new Date('2024/01/01'); // 本地时间

// 陷阱3: 修改日期对象是可变的
const d = new Date();
const d2 = d;
d2.setDate(d2.getDate() + 1);
// d 也被修改了!

// 安全的做法
function addDaysSafe(date, days) {
    const result = new Date(date.getTime()); // 复制
    result.setDate(result.getDate() + days);
    return result;
}

// 陷阱4: 日期比较
const a = new Date('2024-01-01');
const b = new Date('2024-01-01');
console.log(a === b); // false (引用比较)
console.log(a.getTime() === b.getTime()); // true (值比较)
```

<!-- Converted from: 38_日期时间计算.html -->
