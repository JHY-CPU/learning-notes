## 30-整数拆分 (Integer Break)

  dp[i] = max(j*(i-j), j*dp[i-j])

  ## 30-整数拆分 (Integer Break)


```javascript
30-整数拆分 (Integer Break) 概念讲解。dp[i] = max(j*(i-j), j*dp[i-j])```

```
dp[i] = max(j*(i-j), j*dp[i-j])```
