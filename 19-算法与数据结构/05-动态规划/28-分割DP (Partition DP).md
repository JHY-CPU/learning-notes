## 28-分割DP (Partition DP)

  dp[i][j] = min(max(dp[i-1][p], sum[p..j]))

  ## 28-分割DP (Partition DP)


```javascript
28-分割DP (Partition DP) 概念讲解。dp[i][j] = min(max(dp[i-1][p], sum[p..j]))```

```
dp[i][j] = min(max(dp[i-1][p], sum[p..j]))```
