## 03-状态转移方程 (State Transition)

  dp[i] = Math.max(dp[i-1], dp[i-2]+nums[i])

  ## 03-状态转移方程 (State Transition)


```javascript
03-状态转移方程 (State Transition) 概念讲解。dp[i] = Math.max(dp[i-1], dp[i-2]+nums[i])```

```
dp[i] = Math.max(dp[i-1], dp[i-2]+nums[i])```
