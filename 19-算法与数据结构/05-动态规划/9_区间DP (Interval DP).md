## 10-区间DP (Interval DP)

  dp[i][j] = min(dp[i][k] + dp[k+1][j]) + cost

  ## 10-区间DP (Interval DP)


```javascript
10-区间DP (Interval DP) 概念讲解。dp[i][j] = min(dp[i][k] + dp[k+1][j]) + cost```

```
dp[i][j] = min(dp[i][k] + dp[k+1][j]) + cost```
