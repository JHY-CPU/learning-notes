## 13-状态压缩DP (Bitmask DP)

  dp[mask][v] = min(dp[mask\{v}][u] + w(u,v))

  ## 13-状态压缩DP (Bitmask DP)


```javascript
13-状态压缩DP (Bitmask DP) 概念讲解。dp[mask][v] = min(dp[mask\{v}][u] + w(u,v))```

```
dp[mask][v] = min(dp[mask\{v}][u] + w(u,v))```
